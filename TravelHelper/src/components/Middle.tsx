import { MapPage } from "./MapPage";
import { Menu } from "./Menu";

export const Middle = () => {
    return (
        <div className="middle">
            <Menu />
            <MapPage />
        </div>
    );
};
